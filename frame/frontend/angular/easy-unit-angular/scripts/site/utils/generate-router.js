module.exports = function (showCasePath, componentsMap) {
  const routes = [];
  for (const dir in componentsMap) {
    const rowData = componentsMap[dir];
    // const parent = Object.values(rowData.docs)[0].meta;
  }
}
